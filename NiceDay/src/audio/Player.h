#pragma once

/*typedef int SoundID;

// in one layer only one music can play
typedef int MusicLayer;

struct MusicAtribs
{
	MusicLayer layer;
	float startVolume;
	float startingTime;
	
	float volume;
	float endTime;
	
};
class Player
{
public:
	static void init();
	static void playSound(const nd::temp_string& path, float volume);
	static SoundID playMusic(const nd::temp_string& path, const MusicAtribs& atribs);
	static void stopMusic(SoundID id,float fadeOutTime=0);
	static bool isPlaying(SoundID id);
	
	
};*/
