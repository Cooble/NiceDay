#pragma once
/*class Texture;

class TextureManager
{
private:
	static std::unordered_map<const char*, Texture*> s_textures;
public:
	static Texture* getTexture(const char* path);

	static void destroyTextures();

};
*/
