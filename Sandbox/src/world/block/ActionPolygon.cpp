#include "ndpch.h"
#include "ActionPolygon.h"
/*
void PolygonFactory::start()
{
	s_buff.clear();
}

void PolygonFactory::addPoint(WorldPos pos)
{
	s_buff.push_back(pos);
}

void  PolygonFactory::build(ActionFourgon& ref)
{
	ASSERT(s_buff.size() > 4, "Wrong number of points");
	for(int i = 0;i<s_buff.size();i++)
	{
		ref[i] = s_buff[i];
	}
	ref.m_size = s_buff.size();//can be smaller number of points

}
*/
